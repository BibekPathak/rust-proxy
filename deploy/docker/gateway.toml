# SOCKS5 gateway configuration (Docker demo).
bind_addr = "0.0.0.0:1080"
control_plane_url = "http://control-plane:8080"
# api_token = "changeme"
max_connections = 1024
connect_timeout = "10s"
idle_timeout = "300s"
metrics_bind_addr = "0.0.0.0:19090"

[logging]
level = "info"
json = true
